This README file will guide you about all the projects and folders inside the Django folder
Base Path: Desktop -> Sem V -> FSDL - Django -> Webpages -> Django
djenv folder contains the files which make Django work
project1 folder contains two apps app1 and app2. It is first-level where 
the user sees a webpage based on the word after the last '/' in the URL.
project2 folder contains app1 for now. It is second-level where the concept
of templates, variables, if-else statements, conditional loops, etc. is 
used to make webpages powerful and dynamic.